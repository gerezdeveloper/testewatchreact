import './Lineares.css';

const Lineares = (props) => {
    return (
        <div className='linear'>
            <img src={props.imagem} alt='logo linear' />
        </div>
    );
}

export default Lineares;